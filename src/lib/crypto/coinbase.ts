export function createCoinbaseCharge({ name, amount, merchantId }: { name: string; amount: number; merchantId: string }) {
    // This is a placeholder function. You'll need to add the actual
    // logic to interact with the Coinbase API here.
    console.log("createCoinbaseCharge function called with:", { name, amount, merchantId });
  }
  