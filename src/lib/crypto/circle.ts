export function createCircleCharge({ amount, currency, merchantId }: { amount: number; currency: string; merchantId: string }) {
    // This is a placeholder function. You'll need to add the actual
    // logic to interact with the Circle API here.
    console.log("createCircleCharge function called with:", { amount, currency, merchantId });
}
  